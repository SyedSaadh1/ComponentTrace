// import express = require("express");

// let app = express();
// app.use(express.json());
// app.use("api/createComponentMaster", Controller.storeComponentMasterDetails);
// app.use("api/findComponentMaster", Controller.findComponentMasterDetails);
// app.use("api/updateComponentMaster", Controller.updateComponentMasterDetails);
// app.use("api/deleteComponentMaster", Controller.deleteComponentMasterDetails);
// let port = 9000;
// app.listen(port, () => {
//   console.log("server started on ", +port);
// });
